[project]
name = "token-mapping"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets",
    "transformers",
    "scikit-learn",
    "matplotlib",
    "numpy",
    "torch",
    "umap-learn"
]
